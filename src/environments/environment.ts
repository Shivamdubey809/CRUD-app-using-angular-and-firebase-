// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCJgoVH4XerMlvQEVw12ylBubWl4Veeblo",
    authDomain: "angular-client-model.firebaseapp.com",
    databaseURL: "https://angular-client-model.firebaseio.com",
    projectId: "angular-client-model",
    storageBucket: "angular-client-model.appspot.com",
    messagingSenderId: "220356003573",
    appId: "1:220356003573:web:2957366f4272934b"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
